#!/usr/bin/env bash
# nordvpn-helper — Privileged helper for nordvpn CLI
# shellcheck disable=SC2155
#
# Runs as root via sudoers NOPASSWD. Only allows specific, scoped VPN operations.
# Principle of least privilege: the main nordvpn script is unprivileged and
# delegates only the operations that require root to this helper.
#
# DO NOT call this script directly — it is invoked by nordvpn via:
#   sudo -n /usr/local/libexec/nordvpn-helper <command> [args]

set -euo pipefail

PID_FILE="/tmp/nordvpn-openvpn.pid"
LOG_FILE="/tmp/nordvpn-openvpn.log"

_detect_os() {
    case "$(uname -s)" in
        Darwin) ;;
        *) echo "nordvpn-helper: unsupported OS: $(uname -s). nordvpn-cli supports macOS only." >&2; exit 1 ;;
    esac
}

_get_openvpn_bin() {
    if [[ -x "/opt/homebrew/opt/openvpn/sbin/openvpn" ]]; then
        echo "/opt/homebrew/opt/openvpn/sbin/openvpn"
    elif [[ -x "/usr/local/opt/openvpn/sbin/openvpn" ]]; then
        echo "/usr/local/opt/openvpn/sbin/openvpn"
    elif command -v openvpn &>/dev/null; then
        command -v openvpn
    else
        echo "/opt/homebrew/opt/openvpn/sbin/openvpn"
    fi
}

cmd_openvpn_start() {
    local config="$1" auth_file="$2"
    local openvpn_bin
    openvpn_bin=$(_get_openvpn_bin)

    rm -f "$LOG_FILE"
    touch "$LOG_FILE"
    chmod 644 "$LOG_FILE"

    "$openvpn_bin" \
        --config "$config" \
        --auth-user-pass "$auth_file" \
        --daemon "nordvpn" \
        --writepid "$PID_FILE" \
        --log "$LOG_FILE" \
        --connect-retry 3 \
        --connect-timeout 30 \
        --verb 3

    chmod 644 "$PID_FILE" 2>/dev/null || true
}

cmd_openvpn_stop() {
    if [[ -f "$PID_FILE" ]]; then
        local pid
        pid=$(cat "$PID_FILE")
        kill "$pid" 2>/dev/null || true
        for _ in $(seq 1 10); do
            kill -0 "$pid" 2>/dev/null || break
            sleep 1
        done
        if kill -0 "$pid" 2>/dev/null; then
            kill -9 "$pid" 2>/dev/null || true
        fi
        rm -f "$PID_FILE"
    fi
}

cmd_check_pid() {
    [[ -f "$PID_FILE" ]] || exit 1
    local pid
    pid=$(cat "$PID_FILE")
    kill -0 "$pid" 2>/dev/null || exit 1
    echo "$pid"
}

cmd_set_dns() {
    # $1=service, $2=dns1, $3=dns2
    /usr/sbin/networksetup -setdnsservers "$1" "$2" "$3"
    dscacheutil -flushcache 2>/dev/null || true
    killall -HUP mDNSResponder 2>/dev/null || true
}

cmd_clear_dns() {
    for svc in "$@"; do
        /usr/sbin/networksetup -setdnsservers "$svc" Empty 2>/dev/null || true
    done
    dscacheutil -flushcache 2>/dev/null || true
    killall -HUP mDNSResponder 2>/dev/null || true
}

cmd_route_restore() {
    local gateway="$1"
    /sbin/route -n delete -net 0.0.0.0/1    2>/dev/null || true
    /sbin/route -n delete -net 128.0.0.0/1   2>/dev/null || true
    /sbin/route -n delete -net 10.100.0.0/16  2>/dev/null || true
    /sbin/route -n delete default             2>/dev/null || true
    /sbin/route -n add default "$gateway"     2>/dev/null || true
}

# ─── Main dispatch ─────────────────────────────────────────────────────────────

main() {
    local cmd="${1:-}"
    shift || true
    _detect_os

    case "$cmd" in
        openvpn-start)   cmd_openvpn_start "$@" ;;
        openvpn-stop)    cmd_openvpn_stop ;;
        openvpn-kill-all)
            pkill -f "openvpn.*nordvpn" 2>/dev/null || true
            rm -f "$PID_FILE"
            ;;
        check-pid)       cmd_check_pid ;;
        set-dns)         cmd_set_dns "$@" ;;
        clear-dns)       cmd_clear_dns "$@" ;;
        route-restore)   cmd_route_restore "$@" ;;
        *)
            echo "nordvpn-helper: unknown command: $cmd" >&2
            exit 1
            ;;
    esac
}

main "$@"
